<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Verify your email - Fintrack</title>
    <style>
        body {
            font-family: 'Manrope', 'Inter', -apple-system, sans-serif;
            background-color: #0F0F14;
            color: #B4B4C0;
            margin: 0;
            padding: 0;
            line-height: 1.5;
        }
        .wrapper {
            background-color: #0F0F14;
            padding: 60px 20px;
        }
        .container {
            max-width: 440px;
            margin: 0 auto;
            background: linear-gradient(180deg, #1C1C23 0%, #18181D 100%);
            border-radius: 20px;
            border: 1px solid rgba(255, 255, 255, 0.05);
            overflow: hidden;
            box-shadow: 0 40px 80px rgba(0, 0, 0, 0.4);
        }
        .header {
            padding: 40px 32px 24px 32px;
            text-align: center;
        }
        .logo {
            height: 48px;
            width: auto;
            margin-bottom: 12px;
        }
        .brand-name {
            font-size: 18px;
            font-weight: 800;
            color: #FFFFFF;
            letter-spacing: 0.2em;
            margin: 0;
            text-transform: uppercase;
        }
        .content {
            padding: 0px 40px 40px 40px;
            text-align: center;
        }
        .greeting {
            font-size: 20px;
            font-weight: 700;
            color: #FFFFFF;
            margin-bottom: 12px;
        }
        .text {
            font-size: 14px;
            color: #8B8B98;
            margin-bottom: 24px;
        }
        .otp-container {
            background: rgba(124, 122, 255, 0.05);
            border: 1px dashed rgba(124, 122, 255, 0.2);
            border-radius: 12px;
            padding: 24px;
            margin: 24px 0;
        }
        .otp-label {
            font-size: 11px;
            text-transform: uppercase;
            letter-spacing: 0.15em;
            color: #7C7AFF;
            margin-bottom: 8px;
            font-weight: 800;
        }
        .otp-code {
            font-size: 40px;
            font-weight: 800;
            color: #FFFFFF;
            letter-spacing: 0.3em;
            margin: 0;
            text-shadow: 0 0 20px rgba(124, 122, 255, 0.3);
        }
        .footer {
            padding: 24px;
            text-align: center;
            border-top: 1px solid rgba(255, 255, 255, 0.05);
            font-size: 11px;
            color: #5A5A68;
        }
    </style>
</head>
<body>
    <div class="wrapper">
        <div class="container">
            <div class="header">
                <img src="https://res.cloudinary.com/fintrack/image/upload/v1771540001/static/logo_qtfjng.jpg" alt="Fintrack" class="logo">
                <h1 class="brand-name">Fintrack</h1>
            </div>

            <div class="content">
                <p class="greeting">Hello {{firstName}},</p>
                <p class="text">Welcome to the future of finance. Use the secure code below to verify your account.</p>
                
                <div class="otp-container">
                    <p class="otp-label">Security Code</p>
                    <div class="otp-code">{{otp}}</div>
                </div>
            
            </div>

            <div class="footer">
                <p>&copy; 2026 Fintrack Inc. &bull; fintrack.ai</p>
            </div>
        </div>
    </div>
</body>
</html>
