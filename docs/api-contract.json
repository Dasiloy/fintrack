{
  "__comment": "API contract index — each entry points to the authoritative per-controller .doc.json file. Do not duplicate contract details here; maintain them in the files listed below.",
  "convention": "Each controller ships a sibling .doc.json file. HTTP endpoints live next to the NestJS controller; gRPC methods live next to the NestJS microservice controller.",
  "contracts": {
    "api_gateway": {
      "health": "apps/api_gateway/src/app.doc.json",
      "auth": "apps/api_gateway/src/auth/auth.doc.json"
    },
    "auth_service": {
      "auth": "apps/auth_service/src/auth.doc.json"
    }
  }
}
